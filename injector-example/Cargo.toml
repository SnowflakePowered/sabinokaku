[package]
name = "injector-example"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[target.'cfg(windows)'.dependencies]
dll-syringe = { version = "0.1.3", features = ["into_x86_from_x64"] }